import 'package:flutter/material.dart';

class EncabezadoPage extends StatelessWidget {
  const EncabezadoPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}